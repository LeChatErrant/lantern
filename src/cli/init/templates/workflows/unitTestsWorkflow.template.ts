import ProjectPath from '../../../../utils/ProjectPath';
import logger from '../../../../utils/logger';


export default function unitTestsWorkflowTemplate(projectPath: ProjectPath) {
  logger.warn('Unit tests pipeline is not implemented yet (coming soon !)');
}
